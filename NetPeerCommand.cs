namespace NetFlanders
{
    internal enum NetPeerCommand
    {
        RequestConnection,
        ConnectionAccepted,
        ConnectionRejected,
        Disconnect,
        Timeout
    }
}
